services:
  benchmark:
    build:
      context: ..
      dockerfile: benchmark/Dockerfile
    command: ["batch", "--samples", "7", "--warmups", "2"]
    cpus: 2.0
    mem_limit: 1g
    pids_limit: 512
    network_mode: none
    volumes:
      - ./results:/workspace/benchmark/results

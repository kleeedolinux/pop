namespace BenchmarkAllocationChurn

function main()
    local total = 0
    local index = 1
    repeat
        local values = Array.create<<Int>>(256, index)
        total = total + Array.get(values, 1)
        index = index + 1
    until index == 20001
    print(total)
end

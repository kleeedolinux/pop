# Pop.Ffi

Explicit unsafe native-interoperability Package for ADR 0081 ABI types,
statically bound declarations, scoped pins, handles, callbacks, and generated
bindings. It is independently installed, never enters the prelude, and remains
outside `Pop.Standard`.
